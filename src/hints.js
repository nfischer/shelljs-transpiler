var hints = [
  "Try out pipes and redirects:<br><code>cat file.txt | grep \'o\' > a.txt >> b.txt</code>",
  "There's also support for multiline pipes",
  "Try commands like <code>git status</code>",
  "<code>echo $PATH</code>",
  "Bash supports string-manipulation:<br><code>echo ${foo:0:3}</code>",
  "Try out a for-each loop",
  "Drag-and-drop a file into the box to upload it as text",
  "Click 'select-all' to easily copy-paste the output to a file",
  "Try out Bash's <code>$?</code> variable",
  "Try toggling the checkbox above",
  "If you want to use a ShellJS plugin as well, check the boxes below",
  "Concatenate strings and variables:<br><code>echo \"foo${bar}baz\"</code>",
  "Try out unary tests:<br><code>if [ -f file.txt ]...</code>",
  "Try out equality:<br><code>if [ $foo -eq $bar ]...</code>",
  "Try out string length: <code>\"${#foo}\"</code>",
  "This respects reserved words:<br><code>void='some text'; echo $var</code>",
  "Try writing a shebang:<br><code>#!/bin/bash</code>"
];
